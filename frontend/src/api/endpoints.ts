const endpoints = {
    AUTH: {
        "login": "/sign-in",
        "register": "/sign-up",
        "refresh": "/refresh",
        "logout": "/logout"
    },
    PROJECTS: {
        PROD: "data/projects.json",
        TEST: "test.json"
    }
}

export default endpoints